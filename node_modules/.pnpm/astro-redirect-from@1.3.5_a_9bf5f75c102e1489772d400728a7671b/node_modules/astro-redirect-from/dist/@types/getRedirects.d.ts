import type { Redirects } from '.';
export declare function getRedirects(files: string[], srcDir: string, getSlug: (filePath: string) => string, command: 'dev' | 'build' | 'preview' | 'sync', logger: {
    warn: (msg: string) => void;
}): Promise<Redirects>;
